# 基础依赖 - Python 3.11 兼容版本
torch>=2.0.0
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.30.0
gradio>=4.36.0,<5.0.0
pydub>=0.25.0

# CPU 优化
bitsandbytes>=0.41.0; platform_system != "Windows"

# 可选：Flash Attention 加速
# flash-attn>=2.5.0
